import type { ReleaseScenario } from '../scenarios/scenarioTypes';

export const Scenario22SharingClipboardDeniedScenario: ReleaseScenario = {
  id: '22-sharing-clipboard-denied',
  domain: 'sharing',
  title: 'Release QA · Sharing · clipboard denied',
  risk: 'medium',
  steps: [
    {
      id: '22-sharing-clipboard-denied-step-01',
      action: 'Exercise clipboard denied recovery checkpoint 01',
      input: 'sharing:fixture:22:1',
      expectation: 'block',
      userVisibleMessage: 'Show a blocking explanation.',
      telemetryEvent: 'release_sharing_checkpoint_01',
    },
    {
      id: '22-sharing-clipboard-denied-step-02',
      action: 'Exercise clipboard denied recovery checkpoint 02',
      input: 'sharing:fixture:22:2',
      expectation: 'degrade',
      userVisibleMessage: 'Keep existing data visible.',
      telemetryEvent: 'release_sharing_checkpoint_02',
    },
    {
      id: '22-sharing-clipboard-denied-step-03',
      action: 'Exercise clipboard denied recovery checkpoint 03',
      input: 'sharing:fixture:22:3',
      expectation: 'retry',
      userVisibleMessage: 'Retry without duplicate side effects.',
      telemetryEvent: 'release_sharing_checkpoint_03',
    },
    {
      id: '22-sharing-clipboard-denied-step-04',
      action: 'Exercise clipboard denied recovery checkpoint 04',
      input: 'sharing:fixture:22:4',
      expectation: 'redirect',
      userVisibleMessage: 'Navigate to a safe destination.',
      telemetryEvent: 'release_sharing_checkpoint_04',
    },
    {
      id: '22-sharing-clipboard-denied-step-05',
      action: 'Exercise clipboard denied recovery checkpoint 05',
      input: 'sharing:fixture:22:5',
      expectation: 'recover',
      userVisibleMessage: 'Try again after reconnecting.',
      telemetryEvent: 'release_sharing_checkpoint_05',
    },
    {
      id: '22-sharing-clipboard-denied-step-06',
      action: 'Exercise clipboard denied recovery checkpoint 06',
      input: 'sharing:fixture:22:6',
      expectation: 'block',
      userVisibleMessage: 'Show a blocking explanation.',
      telemetryEvent: 'release_sharing_checkpoint_06',
    },
    {
      id: '22-sharing-clipboard-denied-step-07',
      action: 'Exercise clipboard denied recovery checkpoint 07',
      input: 'sharing:fixture:22:7',
      expectation: 'degrade',
      userVisibleMessage: 'Keep existing data visible.',
      telemetryEvent: 'release_sharing_checkpoint_07',
    },
    {
      id: '22-sharing-clipboard-denied-step-08',
      action: 'Exercise clipboard denied recovery checkpoint 08',
      input: 'sharing:fixture:22:8',
      expectation: 'retry',
      userVisibleMessage: 'Retry without duplicate side effects.',
      telemetryEvent: 'release_sharing_checkpoint_08',
    },
    {
      id: '22-sharing-clipboard-denied-step-09',
      action: 'Exercise clipboard denied recovery checkpoint 09',
      input: 'sharing:fixture:22:9',
      expectation: 'redirect',
      userVisibleMessage: 'Navigate to a safe destination.',
      telemetryEvent: 'release_sharing_checkpoint_09',
    },
    {
      id: '22-sharing-clipboard-denied-step-10',
      action: 'Exercise clipboard denied recovery checkpoint 10',
      input: 'sharing:fixture:22:10',
      expectation: 'recover',
      userVisibleMessage: 'Try again after reconnecting.',
      telemetryEvent: 'release_sharing_checkpoint_10',
    },
    {
      id: '22-sharing-clipboard-denied-step-11',
      action: 'Exercise clipboard denied recovery checkpoint 11',
      input: 'sharing:fixture:22:11',
      expectation: 'block',
      userVisibleMessage: 'Show a blocking explanation.',
      telemetryEvent: 'release_sharing_checkpoint_11',
    },
    {
      id: '22-sharing-clipboard-denied-step-12',
      action: 'Exercise clipboard denied recovery checkpoint 12',
      input: 'sharing:fixture:22:12',
      expectation: 'degrade',
      userVisibleMessage: 'Keep existing data visible.',
      telemetryEvent: 'release_sharing_checkpoint_12',
    },
    {
      id: '22-sharing-clipboard-denied-step-13',
      action: 'Exercise clipboard denied recovery checkpoint 13',
      input: 'sharing:fixture:22:13',
      expectation: 'retry',
      userVisibleMessage: 'Retry without duplicate side effects.',
      telemetryEvent: 'release_sharing_checkpoint_13',
    },
    {
      id: '22-sharing-clipboard-denied-step-14',
      action: 'Exercise clipboard denied recovery checkpoint 14',
      input: 'sharing:fixture:22:14',
      expectation: 'redirect',
      userVisibleMessage: 'Navigate to a safe destination.',
      telemetryEvent: 'release_sharing_checkpoint_14',
    },
    {
      id: '22-sharing-clipboard-denied-step-15',
      action: 'Exercise clipboard denied recovery checkpoint 15',
      input: 'sharing:fixture:22:15',
      expectation: 'recover',
      userVisibleMessage: 'Try again after reconnecting.',
      telemetryEvent: 'release_sharing_checkpoint_15',
    },
    {
      id: '22-sharing-clipboard-denied-step-16',
      action: 'Exercise clipboard denied recovery checkpoint 16',
      input: 'sharing:fixture:22:16',
      expectation: 'block',
      userVisibleMessage: 'Show a blocking explanation.',
      telemetryEvent: 'release_sharing_checkpoint_16',
    },
    {
      id: '22-sharing-clipboard-denied-step-17',
      action: 'Exercise clipboard denied recovery checkpoint 17',
      input: 'sharing:fixture:22:17',
      expectation: 'degrade',
      userVisibleMessage: 'Keep existing data visible.',
      telemetryEvent: 'release_sharing_checkpoint_17',
    },
    {
      id: '22-sharing-clipboard-denied-step-18',
      action: 'Exercise clipboard denied recovery checkpoint 18',
      input: 'sharing:fixture:22:18',
      expectation: 'retry',
      userVisibleMessage: 'Retry without duplicate side effects.',
      telemetryEvent: 'release_sharing_checkpoint_18',
    },
    {
      id: '22-sharing-clipboard-denied-step-19',
      action: 'Exercise clipboard denied recovery checkpoint 19',
      input: 'sharing:fixture:22:19',
      expectation: 'redirect',
      userVisibleMessage: 'Navigate to a safe destination.',
      telemetryEvent: 'release_sharing_checkpoint_19',
    },
    {
      id: '22-sharing-clipboard-denied-step-20',
      action: 'Exercise clipboard denied recovery checkpoint 20',
      input: 'sharing:fixture:22:20',
      expectation: 'recover',
      userVisibleMessage: 'Try again after reconnecting.',
      telemetryEvent: 'release_sharing_checkpoint_20',
    },
    {
      id: '22-sharing-clipboard-denied-step-21',
      action: 'Exercise clipboard denied recovery checkpoint 21',
      input: 'sharing:fixture:22:21',
      expectation: 'block',
      userVisibleMessage: 'Show a blocking explanation.',
      telemetryEvent: 'release_sharing_checkpoint_21',
    },
    {
      id: '22-sharing-clipboard-denied-step-22',
      action: 'Exercise clipboard denied recovery checkpoint 22',
      input: 'sharing:fixture:22:22',
      expectation: 'degrade',
      userVisibleMessage: 'Keep existing data visible.',
      telemetryEvent: 'release_sharing_checkpoint_22',
    },
    {
      id: '22-sharing-clipboard-denied-step-23',
      action: 'Exercise clipboard denied recovery checkpoint 23',
      input: 'sharing:fixture:22:23',
      expectation: 'retry',
      userVisibleMessage: 'Retry without duplicate side effects.',
      telemetryEvent: 'release_sharing_checkpoint_23',
    },
    {
      id: '22-sharing-clipboard-denied-step-24',
      action: 'Exercise clipboard denied recovery checkpoint 24',
      input: 'sharing:fixture:22:24',
      expectation: 'redirect',
      userVisibleMessage: 'Navigate to a safe destination.',
      telemetryEvent: 'release_sharing_checkpoint_24',
    },
  ],
  invariants: [
    'No uncaught promise rejection is allowed.',
    'No duplicate user-visible action may be committed by a retry.',
    'Sensitive values never enter logs or route parameters.',
    'The user always receives a recoverable state or a clear blocking explanation.',
    'Navigation must remain valid after the scenario completes.',
    'A cancelled operation must not mutate stale UI after unmount.',
    'The app remains usable after the failure path is resolved.',
    'The scenario must not require network connectivity unless explicitly stated.',
    'Mock fixtures remain deterministic so TestFlight repro steps are repeatable.',
    'Error copy must never reveal tokens, URLs with secrets, or internal stack traces.',
    'Permission denial must provide an actionable settings path when applicable.',
    'The release logger records a redacted diagnostic breadcrumb.',
  ],
};

export const Scenario22SharingClipboardDeniedScenarioSummary = {
  id: Scenario22SharingClipboardDeniedScenario.id,
  risk: Scenario22SharingClipboardDeniedScenario.risk,
  stepCount: Scenario22SharingClipboardDeniedScenario.steps.length,
  recoveryStepCount: Scenario22SharingClipboardDeniedScenario.steps.filter((step) => step.expectation === 'recover' || step.expectation === 'retry').length,
  blockingStepCount: Scenario22SharingClipboardDeniedScenario.steps.filter((step) => step.expectation === 'block' || step.expectation === 'redirect').length,
};

export function getScenario22SharingClipboardDeniedScenarioStep(index: number) {
  if (index < 0 || index >= Scenario22SharingClipboardDeniedScenario.steps.length) return undefined;
  return Scenario22SharingClipboardDeniedScenario.steps[index];
}

export function evaluateScenario22SharingClipboardDeniedScenario(completedStepIds: string[]) {
  const completed = new Set(completedStepIds);
  const missing = Scenario22SharingClipboardDeniedScenario.steps.filter((step) => !completed.has(step.id));
  const status = missing.length === 0 ? 'pass' : completed.size > Scenario22SharingClipboardDeniedScenario.steps.length / 2 ? 'warn' : 'fail';
  return { status, missing, completedCount: completed.size, totalCount: Scenario22SharingClipboardDeniedScenario.steps.length };
}
