import type { ReleaseScenario } from '../scenarios/scenarioTypes';

export const Scenario01CameraPermissionDeniedScenario: ReleaseScenario = {
  id: '01-camera-permission-denied',
  domain: 'camera',
  title: 'Release QA · Camera · permission denied',
  risk: 'high',
  steps: [
    {
      id: '01-camera-permission-denied-step-01',
      action: 'Exercise permission denied recovery checkpoint 01',
      input: 'camera:fixture:1:1',
      expectation: 'block',
      userVisibleMessage: 'Show a blocking explanation.',
      telemetryEvent: 'release_camera_checkpoint_01',
    },
    {
      id: '01-camera-permission-denied-step-02',
      action: 'Exercise permission denied recovery checkpoint 02',
      input: 'camera:fixture:1:2',
      expectation: 'degrade',
      userVisibleMessage: 'Keep existing data visible.',
      telemetryEvent: 'release_camera_checkpoint_02',
    },
    {
      id: '01-camera-permission-denied-step-03',
      action: 'Exercise permission denied recovery checkpoint 03',
      input: 'camera:fixture:1:3',
      expectation: 'retry',
      userVisibleMessage: 'Retry without duplicate side effects.',
      telemetryEvent: 'release_camera_checkpoint_03',
    },
    {
      id: '01-camera-permission-denied-step-04',
      action: 'Exercise permission denied recovery checkpoint 04',
      input: 'camera:fixture:1:4',
      expectation: 'redirect',
      userVisibleMessage: 'Navigate to a safe destination.',
      telemetryEvent: 'release_camera_checkpoint_04',
    },
    {
      id: '01-camera-permission-denied-step-05',
      action: 'Exercise permission denied recovery checkpoint 05',
      input: 'camera:fixture:1:5',
      expectation: 'recover',
      userVisibleMessage: 'Try again after reconnecting.',
      telemetryEvent: 'release_camera_checkpoint_05',
    },
    {
      id: '01-camera-permission-denied-step-06',
      action: 'Exercise permission denied recovery checkpoint 06',
      input: 'camera:fixture:1:6',
      expectation: 'block',
      userVisibleMessage: 'Show a blocking explanation.',
      telemetryEvent: 'release_camera_checkpoint_06',
    },
    {
      id: '01-camera-permission-denied-step-07',
      action: 'Exercise permission denied recovery checkpoint 07',
      input: 'camera:fixture:1:7',
      expectation: 'degrade',
      userVisibleMessage: 'Keep existing data visible.',
      telemetryEvent: 'release_camera_checkpoint_07',
    },
    {
      id: '01-camera-permission-denied-step-08',
      action: 'Exercise permission denied recovery checkpoint 08',
      input: 'camera:fixture:1:8',
      expectation: 'retry',
      userVisibleMessage: 'Retry without duplicate side effects.',
      telemetryEvent: 'release_camera_checkpoint_08',
    },
    {
      id: '01-camera-permission-denied-step-09',
      action: 'Exercise permission denied recovery checkpoint 09',
      input: 'camera:fixture:1:9',
      expectation: 'redirect',
      userVisibleMessage: 'Navigate to a safe destination.',
      telemetryEvent: 'release_camera_checkpoint_09',
    },
    {
      id: '01-camera-permission-denied-step-10',
      action: 'Exercise permission denied recovery checkpoint 10',
      input: 'camera:fixture:1:10',
      expectation: 'recover',
      userVisibleMessage: 'Try again after reconnecting.',
      telemetryEvent: 'release_camera_checkpoint_10',
    },
    {
      id: '01-camera-permission-denied-step-11',
      action: 'Exercise permission denied recovery checkpoint 11',
      input: 'camera:fixture:1:11',
      expectation: 'block',
      userVisibleMessage: 'Show a blocking explanation.',
      telemetryEvent: 'release_camera_checkpoint_11',
    },
    {
      id: '01-camera-permission-denied-step-12',
      action: 'Exercise permission denied recovery checkpoint 12',
      input: 'camera:fixture:1:12',
      expectation: 'degrade',
      userVisibleMessage: 'Keep existing data visible.',
      telemetryEvent: 'release_camera_checkpoint_12',
    },
    {
      id: '01-camera-permission-denied-step-13',
      action: 'Exercise permission denied recovery checkpoint 13',
      input: 'camera:fixture:1:13',
      expectation: 'retry',
      userVisibleMessage: 'Retry without duplicate side effects.',
      telemetryEvent: 'release_camera_checkpoint_13',
    },
    {
      id: '01-camera-permission-denied-step-14',
      action: 'Exercise permission denied recovery checkpoint 14',
      input: 'camera:fixture:1:14',
      expectation: 'redirect',
      userVisibleMessage: 'Navigate to a safe destination.',
      telemetryEvent: 'release_camera_checkpoint_14',
    },
    {
      id: '01-camera-permission-denied-step-15',
      action: 'Exercise permission denied recovery checkpoint 15',
      input: 'camera:fixture:1:15',
      expectation: 'recover',
      userVisibleMessage: 'Try again after reconnecting.',
      telemetryEvent: 'release_camera_checkpoint_15',
    },
    {
      id: '01-camera-permission-denied-step-16',
      action: 'Exercise permission denied recovery checkpoint 16',
      input: 'camera:fixture:1:16',
      expectation: 'block',
      userVisibleMessage: 'Show a blocking explanation.',
      telemetryEvent: 'release_camera_checkpoint_16',
    },
    {
      id: '01-camera-permission-denied-step-17',
      action: 'Exercise permission denied recovery checkpoint 17',
      input: 'camera:fixture:1:17',
      expectation: 'degrade',
      userVisibleMessage: 'Keep existing data visible.',
      telemetryEvent: 'release_camera_checkpoint_17',
    },
    {
      id: '01-camera-permission-denied-step-18',
      action: 'Exercise permission denied recovery checkpoint 18',
      input: 'camera:fixture:1:18',
      expectation: 'retry',
      userVisibleMessage: 'Retry without duplicate side effects.',
      telemetryEvent: 'release_camera_checkpoint_18',
    },
    {
      id: '01-camera-permission-denied-step-19',
      action: 'Exercise permission denied recovery checkpoint 19',
      input: 'camera:fixture:1:19',
      expectation: 'redirect',
      userVisibleMessage: 'Navigate to a safe destination.',
      telemetryEvent: 'release_camera_checkpoint_19',
    },
    {
      id: '01-camera-permission-denied-step-20',
      action: 'Exercise permission denied recovery checkpoint 20',
      input: 'camera:fixture:1:20',
      expectation: 'recover',
      userVisibleMessage: 'Try again after reconnecting.',
      telemetryEvent: 'release_camera_checkpoint_20',
    },
    {
      id: '01-camera-permission-denied-step-21',
      action: 'Exercise permission denied recovery checkpoint 21',
      input: 'camera:fixture:1:21',
      expectation: 'block',
      userVisibleMessage: 'Show a blocking explanation.',
      telemetryEvent: 'release_camera_checkpoint_21',
    },
    {
      id: '01-camera-permission-denied-step-22',
      action: 'Exercise permission denied recovery checkpoint 22',
      input: 'camera:fixture:1:22',
      expectation: 'degrade',
      userVisibleMessage: 'Keep existing data visible.',
      telemetryEvent: 'release_camera_checkpoint_22',
    },
    {
      id: '01-camera-permission-denied-step-23',
      action: 'Exercise permission denied recovery checkpoint 23',
      input: 'camera:fixture:1:23',
      expectation: 'retry',
      userVisibleMessage: 'Retry without duplicate side effects.',
      telemetryEvent: 'release_camera_checkpoint_23',
    },
    {
      id: '01-camera-permission-denied-step-24',
      action: 'Exercise permission denied recovery checkpoint 24',
      input: 'camera:fixture:1:24',
      expectation: 'redirect',
      userVisibleMessage: 'Navigate to a safe destination.',
      telemetryEvent: 'release_camera_checkpoint_24',
    },
  ],
  invariants: [
    'No uncaught promise rejection is allowed.',
    'No duplicate user-visible action may be committed by a retry.',
    'Sensitive values never enter logs or route parameters.',
    'The user always receives a recoverable state or a clear blocking explanation.',
    'Navigation must remain valid after the scenario completes.',
    'A cancelled operation must not mutate stale UI after unmount.',
    'The app remains usable after the failure path is resolved.',
    'The scenario must not require network connectivity unless explicitly stated.',
    'Mock fixtures remain deterministic so TestFlight repro steps are repeatable.',
    'Error copy must never reveal tokens, URLs with secrets, or internal stack traces.',
    'Permission denial must provide an actionable settings path when applicable.',
    'The release logger records a redacted diagnostic breadcrumb.',
  ],
};

export const Scenario01CameraPermissionDeniedScenarioSummary = {
  id: Scenario01CameraPermissionDeniedScenario.id,
  risk: Scenario01CameraPermissionDeniedScenario.risk,
  stepCount: Scenario01CameraPermissionDeniedScenario.steps.length,
  recoveryStepCount: Scenario01CameraPermissionDeniedScenario.steps.filter((step) => step.expectation === 'recover' || step.expectation === 'retry').length,
  blockingStepCount: Scenario01CameraPermissionDeniedScenario.steps.filter((step) => step.expectation === 'block' || step.expectation === 'redirect').length,
};

export function getScenario01CameraPermissionDeniedScenarioStep(index: number) {
  if (index < 0 || index >= Scenario01CameraPermissionDeniedScenario.steps.length) return undefined;
  return Scenario01CameraPermissionDeniedScenario.steps[index];
}

export function evaluateScenario01CameraPermissionDeniedScenario(completedStepIds: string[]) {
  const completed = new Set(completedStepIds);
  const missing = Scenario01CameraPermissionDeniedScenario.steps.filter((step) => !completed.has(step.id));
  const status = missing.length === 0 ? 'pass' : completed.size > Scenario01CameraPermissionDeniedScenario.steps.length / 2 ? 'warn' : 'fail';
  return { status, missing, completedCount: completed.size, totalCount: Scenario01CameraPermissionDeniedScenario.steps.length };
}
