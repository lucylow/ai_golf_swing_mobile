import React from "react";
import { Pressable, StyleSheet, Text, View } from "react-native";
import { FIGMA } from "../theme";
import { Badge } from "./Badge";
export function PracticeSessionCard({ date, club, score, focus, swings, onPress }: { date: string; club: string; score: number; focus: string; swings: number; onPress?: () => void }) { return <Pressable onPress={onPress} style={({ pressed }) => [styles.card, pressed && styles.pressed]}><View style={styles.top}><Badge label={club} tone="neutral" compact /><Text style={styles.date}>{date}</Text></View><View style={styles.bottom}><View><Text style={styles.title}>{focus}</Text><Text style={styles.meta}>{swings} swings · AI review</Text></View><Text style={styles.score}>{score}</Text></View></Pressable>; }
const styles = StyleSheet.create({ card: { padding: 12, borderRadius: 15, borderWidth: 1, borderColor: FIGMA.colors.border, backgroundColor: FIGMA.colors.surface, gap: 12 }, top: { flexDirection: "row", justifyContent: "space-between", alignItems: "center" }, date: { color: FIGMA.colors.muted, fontSize: 8 }, bottom: { flexDirection: "row", justifyContent: "space-between", alignItems: "flex-end" }, title: { color: FIGMA.colors.white, fontSize: 13, fontWeight: "900" }, meta: { color: FIGMA.colors.muted, fontSize: 8, marginTop: 3 }, score: { color: FIGMA.colors.lime, fontSize: 25, fontWeight: "900" }, pressed: { opacity: 0.72 } });
